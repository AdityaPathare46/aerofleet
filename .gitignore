# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
/build/
develop-eggs/
dist/
eggs/
.eggs/
# Anchored to repo root — these entries describe top-level Python
# packaging-build artifacts (setup.py build/develop), not a name convention
# safe to blanket-ignore anywhere in the tree. Unanchored `lib/`/`lib64/`
# already silently swallowed tauri-app/src/lib/ once, and unanchored
# `downloads/` did the same to landing/downloads/ — both caught only
# because a real file failed to show up in `git status` before a commit.
# `dist/` stays unanchored on purpose: tauri-app/dist/ (the Vite build
# output) already relies on it being ignored at that depth too.
/lib/
/lib64/
/downloads/
parts/
sdist/
var/
wheels/
pip-wheel-metadata/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
venv/
env/
ENV/
env.bak/
venv.bak/
.venv/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
coverage.xml
*.cover
.hypothesis/

# Jupyter
.ipynb_checkpoints
*.ipynb

# Environment Variables
.env
.env.local
.env.*.local

# Logs
*.log
logs/

# Database
*.db
*.sqlite
*.sqlite3
db.sqlite3

# Cached Data
*.pkl
*.pickle
*.cache

# Mission Data (generated)
saved_missions/*.json
!saved_missions/.gitkeep

# Reports (generated)
*.pdf
!docs/*.pdf

# Temporary Files
tmp/
temp/
*.tmp

# OS Files
.DS_Store
Thumbs.db
desktop.ini

# Documentation Build
docs/_build/
docs/_static/
docs/_templates/

# Docker
.dockerignore

# Monitoring & Metrics
prometheus_data/
grafana_data/

# Alembic
alembic/versions/*.pyc

# Vector Database
src/vector_index.pkl
*.faiss

# Legacy orbital-mechanics datasets (24GB+ of SPICE kernels, gravity models,
# NASA report corpora) inherited from the pre-pivot project — unreferenced
# by the live aerofleet drone-dispatch app (Phase AJ confirmed only the now-
# removed aerofleet/data/loader.py touched these). Too large for git and not
# needed to run or test AeroFleet.
datasets/
data/docs/

# Regeneratable runtime caches — osmnx geocoding/street-graph cache and the
# embeddings vector DB. Rebuilt automatically on first run; committing them
# just bloats the repo with binary blobs that go stale immediately.
cache/
data/city_cache/
data/vector_db/

# Generated scenario-run reports (dated JSON/txt output) — reproducible by
# re-running scenario_engine, not source.
scenario_reports/

# Mac OS metadata files
._*

# Node modules and Rust builds
node_modules/
target/
